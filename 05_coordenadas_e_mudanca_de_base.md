**# Coordenadas e Mudança de Base

Dada uma base ordenada \( B = \{v_1, \dots, v_n\} \), as **coordenadas** de um vetor \( x \) relativamente a \( B \) são os coeficientes \( [x]_B = (c_1, \dots, c_n) \) tais que:

\[
x = c_1 v_1 + \dots + c_n v_n
\]

A **matriz de mudança de base** de \( B \) para \( C \) permite converter coordenadas:

\[
[x]_C = P_{C \leftarrow B} [x]_B
\]**
